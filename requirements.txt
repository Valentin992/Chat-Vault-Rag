# Paso 1 (chunking) NO necesita nada: corre con Python estándar.

# Paso 2 — embeddings (OpenAI) + carga de la API key:
openai
python-dotenv

# Paso 3 — vector DB:
chromadb

# Paso 5 — generación con Claude:
anthropic

# Paso 6 — interfaz:
streamlit

# Paso 7a — evals (pydantic valida el veredicto del juez LLM):
pydantic

# Deploy (Streamlit Cloud / Linux): ChromaDB needs sqlite3 >= 3.35, but the
# hosted Linux image ships an older one. This wheel provides a modern build.
# Installed ONLY on Linux — never on local Windows/Mac (no wheel there anyway).
pysqlite3-binary; sys_platform == "linux"
